---
description: Efficiency — avoid redundant work, optimise critical paths, lean bundle
alwaysApply: true
---

# Efficiency (Target: 100%)

- Avoid unnecessary API calls — batch where possible; do not re-fetch unchanged data.
- Avoid redundant loops, duplicate validation, or repeated computations.
- Use efficient algorithms and data structures (O(n) where reasonable).
- Memoize expensive pure functions; use `useCallback`/`useMemo` for stable handlers.
- Lazy-load heavy components when appropriate.
- Do not sacrifice readability for micro-optimisations.

**Checklist:**
- [ ] Insight GET avoids costly AI calls; pattern analysis via explicit POST only.
- [ ] Scores computed once server-side, not duplicated client-side.
- [ ] No N+1 patterns or redundant schema parsing.
- [ ] Bundle kept lean — tree-shake unused imports.
- [ ] Loading states prevent duplicate submissions.
